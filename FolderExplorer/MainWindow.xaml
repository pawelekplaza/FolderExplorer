<Window x:Class="FolderExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderExplorer"
        xmlns:converter="clr-namespace:FolderExplorer.Converters"
        mc:Ignorable="d"
        Title="Folder explorer" Height="350" Width="525">

    <Window.Resources>
        <converter:ItemToImageConverter x:Key="itemToImage" />
        <converter:ItemToNameConverter x:Key="itemToName" />
    </Window.Resources>
    
    <Grid>

        <TreeView ItemsSource="{Binding Items}">

            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                </Style>
            </TreeView.ItemContainerStyle>

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Width="20" Margin="3" Source="{Binding Converter={StaticResource itemToImage}}" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding Converter={StaticResource itemToName}}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>

    </Grid>
</Window>
